{
  "name": "parasmaninx-shop",
  "version": "1.0.0",
  "description": "ParasmaniNX online cloth shop",
  "main": "server.js",
  "scripts": {
    "start": "node --no-warnings server.js",
    "dev": "node --no-warnings --watch server.js",
    "seed": "node --no-warnings src/seed.js",
    "test": "node --no-warnings --test test/*.test.js"
  },
  "dependencies": {
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "multer": "^2.4.0"
  },
  "private": true,
  "engines": {
    "node": ">=22.5"
  }
}
